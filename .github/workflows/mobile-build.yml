name: Mobile - Lint, Typecheck & Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: mobile-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ---------------------------------------------------------------------------
  # Lint & Typecheck
  # ---------------------------------------------------------------------------
  lint-and-typecheck:
    name: Lint & Typecheck
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Cache node_modules
        uses: actions/cache@v4
        id: npm-cache
        with:
          path: |
            node_modules
            apps/mobile/node_modules
            packages/shared/node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-modules-

      - name: Install dependencies
        if: steps.npm-cache.outputs.cache-hit != 'true'
        run: npm ci

      - name: Lint mobile app
        run: npm run lint --workspace=apps/mobile

      - name: Typecheck mobile app
        run: npm run typecheck --workspace=apps/mobile

  # ---------------------------------------------------------------------------
  # EAS Build (placeholder)
  # ---------------------------------------------------------------------------
  eas-build:
    name: EAS Build
    runs-on: ubuntu-latest
    needs: lint-and-typecheck
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Cache node_modules
        uses: actions/cache@v4
        id: npm-cache
        with:
          path: |
            node_modules
            apps/mobile/node_modules
            packages/shared/node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-modules-

      - name: Install dependencies
        if: steps.npm-cache.outputs.cache-hit != 'true'
        run: npm ci

      - name: Setup EAS CLI
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      # Placeholder -- replace the profile and platform flags as needed.
      - name: Build with EAS
        working-directory: apps/mobile
        run: |
          echo "=== EAS Build Placeholder ==="
          echo "In production, run:"
          echo "  eas build --platform all --profile preview --non-interactive"
          echo "Skipping actual build in CI for now."
          # Uncomment the line below when EAS is configured:
          # eas build --platform all --profile preview --non-interactive
